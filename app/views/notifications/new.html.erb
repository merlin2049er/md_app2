<% @user = current_user %>
<% if user_signed_in? && @user.admin %>


<!-- <%# if can? :manage, @notification %> -->


<h1>New Notification</h1>

<%= render 'form', notification: @notification %>

<%= link_to 'Back', notifications_path %>



<% else %>


    <div class="alert alert-warning" role="alert">Please sign in as administrator to add notifications.</div>

<% end %>
