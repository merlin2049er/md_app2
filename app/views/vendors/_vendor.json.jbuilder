json.extract! vendor, :id, :vendor, :contact_name, :address1, :address2, :website, :product, :dropship, :processing, :locally, :payments, :created_at, :updated_at
json.url vendor_url(vendor, format: :json)
